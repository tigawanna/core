{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/gulp/*"
  ],
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "lerna": "^8.1.2",
    "prettier": "3.9.6"
  }
}
